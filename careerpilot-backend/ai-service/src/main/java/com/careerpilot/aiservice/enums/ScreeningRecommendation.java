package com.careerpilot.aiservice.enums;

/** Matches your spec's four screening recommendation buckets exactly. */
public enum ScreeningRecommendation {
    HighlyRecommended,
    Recommended,
    Consider,
    NotRecommended
}
