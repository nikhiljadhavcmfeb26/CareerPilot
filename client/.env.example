# Base URL of the Spring Cloud API Gateway.
# The React app must never talk to a microservice port (8081-8086) directly.
#
# Copy this file to `.env` and adjust if your gateway runs elsewhere.
VITE_API_BASE_URL=http://localhost:8080/api
